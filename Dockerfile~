# Utiliser l'image officielle Tomcat comme base
FROM ibtech/tomcat9

# Désactiver les applications par défaut de Tomcat
# RUN rm -rf /usr/local/tomcat/webapps/*

# Copier ton fichier WAR dans le répertoire webapps de Tomcat
# COPY target/CalculWeb.war /usr/local/tomcat/webapps/CalculWeb.war

# Exposer le port 8080 (port par défaut de Tomcat)
EXPOSE 8080

# Lancer Tomcat en mode de premier plan
CMD ["catalina.sh", "run"]
